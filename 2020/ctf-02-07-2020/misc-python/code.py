print("Something is wrong with this code ...")

def get_flag(flag, bites):
        print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
        flag = [a ^ bites[(0 * 12 + 19) % len(bites)] for a in flag]
	flag = [a ^ bites[(1 * 4 + 13) % len(bites)] for a in flag]
	flag = [a ^ bites[(2 * 9 + 14) % len(bites)] for a in flag]
        print("jaVa > python 2 > PyThhON 3 > lISP > c+++")
	flag = [a ^ bites[(3 * 12 + 0) % len(bites)] for a in flag]
	flag = [a ^ bites[(4 * 16 + 0) % len(bites)] for a in flag]
        print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
	flag = [a ^ bites[(5 * 19 + 7) % len(bites)] for a in flag]
        flag = [a ^ bites[(6 * 18 + 12) % len(bites)] for a in flag]
        print("Roses are #ff0000")
	flag = [a ^ bites[(7 * 6 + 13) % len(bites)] for a in flag]
        flag = [a ^ bites[(8 * 0 + 20) % len(bites)] for a in flag]
        flag = [a ^ bites[(9 * 20 + 9) % len(bites)] for a in flag]
        print("im majoring in CS:GO")
        flag = [a ^ bites[(10 * 7 + 12) % len(bites)] for a in flag]
	print("#7f00ff are #0000ff")
        flag = [a ^ bites[(11 * 12 + 16) % len(bites)] for a in flag]
        flag = [a ^ bites[(12 * 20 + 19) % len(bites)] for a in flag]
	print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
        flag = [a ^ bites[(13 * 12 + 8) % len(bites)] for a in flag]
	flag = [a ^ bites[(14 * 20 + 1) % len(bites)] for a in flag]
        flag = [a ^ bites[(15 * 17 + 11) % len(bites)] for a in flag]
        print("thIs woRkeD on mY mAchiNe")
	flag = [a ^ bites[(16 * 0 + 9) % len(bites)] for a in flag]
	print("im majoring in CS:GO")
        flag = [a ^ bites[(17 * 1 + 18) % len(bites)] for a in flag]
        print("stakc under flow")
        flag = [a ^ bites[(18 * 12 + 16) % len(bites)] for a in flag]
	flag = [a ^ bites[(19 * 7 + 7) % len(bites)] for a in flag]
        flag = [a ^ bites[(20 * 13 + 5) % len(bites)] for a in flag]
	print("#7f00ff are #0000ff")
	flag = [a ^ bites[(21 * 14 + 16) % len(bites)] for a in flag]
	print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
	flag = [a ^ bites[(22 * 4 + 12) % len(bites)] for a in flag]
	print("how to haCK ti 84")
	flag = [a ^ bites[(23 * 14 + 17) % len(bites)] for a in flag]
	flag = [a ^ bites[(24 * 2 + 17) % len(bites)] for a in flag]
        print(" C S 3 6 1 ")
        flag = [a ^ bites[(25 * 5 + 19) % len(bites)] for a in flag]
        print("how to haCK ti 84")
        flag = [a ^ bites[(26 * 17 + 18) % len(bites)] for a in flag]
	print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
	flag = [a ^ bites[(27 * 12 + 15) % len(bites)] for a in flag]
	flag = [a ^ bites[(28 * 8 + 4) % len(bites)] for a in flag]
        print("thIs woRkeD on mY mAchiNe")
        flag = [a ^ bites[(29 * 2 + 5) % len(bites)] for a in flag]
	flag = [a ^ bites[(30 * 8 + 19) % len(bites)] for a in flag]
        print("stakc under flow")
	flag = [a ^ bites[(31 * 1 + 11) % len(bites)] for a in flag]
        print("im majoring in CS:GO")
	flag = [a ^ bites[(32 * 12 + 7) % len(bites)] for a in flag]
        print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
        flag = [a ^ bites[(33 * 19 + 19) % len(bites)] for a in flag]
	flag = [a ^ bites[(34 * 19 + 7) % len(bites)] for a in flag]
	print("how to haCK ti 84")
        flag = [a ^ bites[(35 * 2 + 8) % len(bites)] for a in flag]
	print("jaVa > python 2 > PyThhON 3 > lISP > c+++")
	flag = [a ^ bites[(36 * 18 + 18) % len(bites)] for a in flag]
        flag = [a ^ bites[(37 * 10 + 0) % len(bites)] for a in flag]
	flag = [a ^ bites[(38 * 8 + 7) % len(bites)] for a in flag]
	flag = [a ^ bites[(39 * 7 + 8) % len(bites)] for a in flag]
        flag = [a ^ bites[(40 * 20 + 9) % len(bites)] for a in flag]
        print("utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{utflag{}}}}}}}}}")
        flag = [a ^ bites[(41 * 0 + 15) % len(bites)] for a in flag]
        flag = [a ^ bites[(42 * 0 + 17) % len(bites)] for a in flag]
        flag = [a ^ bites[(43 * 4 + 11) % len(bites)] for a in flag]
        print("jaVa > python 2 > PyThhON 3 > lISP > c+++")
        flag = [a ^ bites[(44 * 12 + 1) % len(bites)] for a in flag]
	flag = [a ^ bites[(45 * 3 + 6) % len(bites)] for a in flag]
	flag = [a ^ bites[(46 * 0 + 5) % len(bites)] for a in flag]
        flag = [a ^ bites[(47 * 14 + 10) % len(bites)] for a in flag]
	print("thIs woRkeD on mY mAchiNe")
        flag = [a ^ bites[(48 * 11 + 12) % len(bites)] for a in flag]
        flag = [a ^ bites[(49 * 0 + 3) % len(bites)] for a in flag]
	return "".join([chr(a) for a in flag])

flag = [2, 3, 17, 27, 22, 16, 12, 70, 25, 19, 68, 25, 3, 22, 3, 70, 71, 25, 40, 30, 4, 40, 54, 40, 4, 71, 20, 30, 67, 27, 40, 52, 71, 25, 66, 3, 5, 2, 20, 35, 10]
bites = [187, 160, 250, 6, 36, 157, 59, 63, 166, 37, 43, 195, 120, 38, 211, 191, 83, 195, 111, 170]

print(get_flag(flag, bites))
